# DEPRECATED — pipeline dependencies are now in backend/requirements.txt.
# Kept only so `pip install -r scripts/requirements-scraper.txt` still works
# for anyone with old bookmarks.
-r ../backend/requirements.txt
